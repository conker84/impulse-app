__pycache__/
*.pyc
.venv/
node_modules/
.claude/
reports/
*.egg-info/
.DS_Store
CLAUDE.md
.databricks/
uv.lock
.vscode/
frontend/dist/
# profiles.yaml is customer-specific schema mapping; ship profiles.yaml.example
# in git (task #9) and let customers commit their own profiles.yaml in their
# fork. Bundle deploy uploads whatever's in the repo tree.
profiles.yaml

# install.sh writes the customer's catalog/schema choices here for re-runs.
.install.config
